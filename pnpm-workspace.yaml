packages:
  - "."
overrides:
  nanoid: ">=3.3.18"
allowBuilds:
  esbuild: true
  # MSW's optional postinstall only prints a sponsorship message; no runtime
  # artifact is required, so CI deliberately keeps it disabled.
  msw: false
supportedArchitectures:
  os:
    - linux
    - win32
  cpu:
    - x64
    - arm64
  libc:
    - glibc
